Sources for the plugin `plugin.att` (App Tracking Transparency)

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.att" =
            publisherId = "com.solar2d",
        },
    },
}
```
